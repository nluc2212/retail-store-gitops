apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: retail-store-gateway
  namespace: istio-system
spec:
  selector:
    app: istio-ingress
  servers:
  - port:
      number: 80
      name: http
      protocol: HTTP
    hosts:
    - "*"
---
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: retail-store-vs
  namespace: default
spec:
  hosts:
  - "*"
  gateways:
  - istio-system/retail-store-gateway
  http:
  - match:
    - uri:
        prefix: /
    route:
    - destination:
        host: ui.default.svc.cluster.local
        port:
          number: 80
